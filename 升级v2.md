将现有代码升级成 **V2版本**，目标如下：

```
                   ┌─────────────────────┐
                   │   Watchdog线程      │
                   └─────────┬───────────┘
                             │
                   FileEvent Queue
                             │
                             ▼
                 ┌─────────────────────┐
                 │ EventProcessor线程  │
                 └─────────┬───────────┘
                           │
              Browser Queue│
                           ▼
                ┌─────────────────────┐
                │ Browser Monitor线程 │
                └─────────┬───────────┘
                          │
                     Log Queue
                          │
                          ▼
                 ┌─────────────────────┐
                 │ SQLite Writer线程   │
                 └─────────────────────┘
```
```

WorkMonitor/
│
├── main.py                 # 程序入口
├── config.py               # 配置读取
├── database.py             # SQLite Writer（唯一数据库线程）
├── events.py               # 所有事件定义
├── browser.py              # 浏览器监听
├── filesystem.py           # Watchdog监听
├── processor.py            # 事件处理中心
├── report.py               # 日报生成
├── tray.py                 # 系统托盘
├── gui.py                  # tkinter窗口
├── models.py               # dataclass
├── utils.py
│
├── config.json
└── worklog.db

```

这样有几个优势：

* **数据库只保留一个连接**（不再每次 connect/close）
* **取消 DB_LOCK**
* **所有日志批量写入 SQLite**
* **浏览器监控和文件监控彻底解耦**
* **事件不会阻塞 Watchdog**
* **CPU 占用下降**
* **SQLite 写入速度提升约 5～20 倍**
* **以后增加 Outlook、微信、VSCode 等监控几乎不用改架构**

另外做这些优化：

* 去掉已经没实际作用的 Everything 查询代码
* `pending_close` 改为线程安全实现
* `browser_monitor` 去掉重复代码
* SQLite 开启 WAL
* 增加索引
* 日报统计一次遍历完成
* 配置改成 dataclass
* 日志统一封装
* 文件状态改成 dataclass
* 所有数据库 SQL 集中管理
* 完整类型注解
* 完整异常处理
* Python 3.11 风格重构

---

预计变化：

| 项目   | 当前         | V2         |
| ---- | ---------- | ---------- |
| 文件数  | 1 个（713 行） | 8～12 个模块   |
| 数据库  | 每次连接       | 单连接 Writer |
| 线程   | 4 个        | 5 个（事件驱动）  |
| CPU  | 中等         | 更低         |
| 可维护性 | 一般         | 很高         |
| 扩展功能 | 困难         | 很容易        |

目录会类似：

```
work_monitor/
│
├── main.py
├── config.py
├── database.py
├── browser.py
├── watchdog_handler.py
├── event_processor.py
├── report.py
├── models.py
├── utils.py
└── config.json
```

---
结构清晰、性能更高、后续维护容易，保留现有的所有功能（CAD/Office 识别、浏览器统计、日报生成等），同时兼容现有的 `config.json` 和 SQLite 数据库。

我会保留的功能（100%兼容）

✅ CAD 打开/关闭识别

✅ Office 打开关闭识别

✅ 修改保存统计

✅ 浏览网页统计

✅ 自动日报

✅ 排除目录

✅ 排除文件

✅ 托盘运行

✅ SQLite

会新增的功能
① 数据库写入线程（最大的优化）

现在你的代码是

Watchdog
    ↓
SQLite

以后变成

Watchdog
      ↓
 Queue
      ↓
SQLite Writer

数据库永远只有一个连接。

速度会快很多。

② 所有事件统一

现在你的代码有

打开文件

关闭文件

修改

浏览网页

以后全部变成

Event(
    type="modify",
    time=...
    path=...
    size=...
)

统一处理。

以后增加微信、Outlook、VSCode几乎不用改代码。

③ dataclass

例如现在

self.file_states[path]={
    "open_time":...
    "initial_size":...
}

以后

@dataclass
class FileState:

    open_time:datetime

    initial_size:int

    last_size:int

    modified:int

可读性提高很多。

④ Browser重写

你现在 Browser Monitor 大约 70 行，而且有两段几乎一样的数据库代码。

以后：

BrowserMonitor

↓

PageTracker

↓

LogEvent

↓

SQLite Writer

重复代码基本没有。

⑤ EventProcessor

这是整个系统最大的变化。

以前

Watchdog

↓

数据库

以后

Watchdog
        ↓
 Event Queue
        ↓
Event Processor
        ↓
Browser Event
        ↓
 DB Queue
        ↓
SQLite

所有业务逻辑全部集中。

⑥ SQLite优化

自动开启

WAL

NORMAL

cache_size

temp_store=MEMORY

写速度明显提升。

⑦ 批量写数据库

现在：

1 条事件

commit

以后

100 条

一次 commit

性能提升非常明显。

⑧ 去掉DB_LOCK

数据库只有一个线程。

因此：

DB_LOCK

全部删除。

⑨ 日报优化

现在日报要循环很多遍。

以后：

一次遍历

统计全部结果

CPU降低很多。

⑩ 日志模块

以后统一

logger.info()

logger.warning()

logger.error()

不再使用

print()
后续还能非常容易增加

例如：

✅ 微信聊天时间

✅ QQ

✅ Outlook

✅ VSCode

✅ AutoCAD

✅ Excel

✅ Word

✅ 浏览器URL（而不是标题）

✅ Git提交统计

✅ AI使用统计

全部只需要新增一个模块。

我还准备顺便升级代码质量

包括：

全部增加类型注解（typing）
使用 pathlib.Path 替代大量 os.path
统一异常处理
配置对象化（dataclass）
SQL 集中管理
消除重复代码
模块职责单一，便于测试